language: cpp

sudo: required
dist: trusty

before_install:
 - sudo add-apt-repository --yes ppa:ubuntu-sdk-team/ppa
 - sudo apt-get update -qq
 - sudo apt-get install qtbase5-dev qtdeclarative5-dev libqt5webkit5-dev libsqlite3-dev
 - sudo apt-get install qt5-default qttools5-dev-tools

install:
  - sudo apt-get update
  - sudo apt-get install qt5-default qttools5-dev-tools
  
script:
  #- qmake "CONFIG+=test" iRobot.pro #we gonna compile for Unit test first
  #- make
  #- ./iRobot #run unit test
  #- rm Makefile iRobot *.o #remove files (which are generated from unit test) for next build
  - qmake CONFIG+=debug BigBlueBox.pro #we gonna compile for actual program
  - make
  # test complete
 

